# deep_packet_analyzer
Designed and implemented a multi-threaded Deep Packet Inspection (DPI) engine capable of real-time packet analysis, protocol detection (HTTP, HTTPS, DNS), SNI extraction, and rule-based traffic filtering using load balancing and fast path processing.
